import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  contentContainer: {
    paddingVertical: 40,
    paddingRight: 20
  },

  separator: {
    width: 20, // gap entre os items
  }
})